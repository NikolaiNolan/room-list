image: node:8.16.0

pipelines:
  branches:
    master:
      - step:
          caches:
            - node
          script: # Modify the commands below to build your repository.
            - cd functions
            - yarn install
            - cd ..
            - yarn install
            - yarn deploy
