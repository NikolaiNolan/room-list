image: node:8.16.0

pipelines:
  branches:
    master:
      - step:
          caches:
            - node
            - firebase-hosting
          script:
            - yarn install
            - yarn deploy-content

definitions:
  caches:
    firebase-hosting: .firebase
